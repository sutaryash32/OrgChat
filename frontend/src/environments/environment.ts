export const environment = {
  production: false,
  apiUrl: `${window.location.origin}/api`,
  wsUrl: `${window.location.origin}/ws`,
  googleAuthUrl: `${window.location.origin}/oauth2/authorization/google`
};
